using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 시나리오 1
/// 1.차량 진입 
/// 2.주유자 옷차림
/// 3.작업 환경
/// 4.
/// 5.
/// 6.
/// 7.
/// </summary>
public class Scenario_Manager : MonoBehaviour
{
    [SerializeField] private List<ScenarioObj> scenarios1 = new List<ScenarioObj>();
    [SerializeField] private List<ScenarioObj> scenarios2 = new List<ScenarioObj>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
