using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VolumePopup : MonoBehaviour
{
    [SerializeField] private VolumeGroup alarmGroup;
    [SerializeField] private VolumeGroup alarm2Group;
    [SerializeField] private VolumeGroup sirenGroup;
    [SerializeField] private VolumeGroup broadcastGroup;
    [SerializeField] private VolumeGroup buzzerGroup;


    private void Awake()
    {
        
    }
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
